# Energy, 2026-09-11

Droppy Playground 1.0.11,
macOS 26.6.2, the `DroppyPlayground` process only,
60 samples a second apart per run after 20 s to settle, and the machine was otherwise quiet.
The pointer was not parked; wherever it was, it was there for all three runs.

| Run | Idle CPU (%) | Idle wakeups (a second) | POWER | Seat | Clock started | Against removed |
|---|---|---|---|---|---|---|
| removed | 0.363 | 2.067 | 0.462 | | | baseline |
| seated (OW_DEMO=1, pinned) | 0.330 | 1.933 | 0.427 | compact | 2 | pass |
| unseated (OW_DEMO=1, unpinned) | 0.320 | 1.883 | 0.422 | none logged | 0 | pass |

The line: a run with the droplet in it may wake the Playground at most
max(0.5, 0.2 of the baseline) a second more than the removed run, and
cost at most 0.1% more CPU; the unseated run may start no clock.

The droplet activated in both runs it was installed for.
Unseated and unshelved: pass: no clock started in the unseated run (seat never assigned).
